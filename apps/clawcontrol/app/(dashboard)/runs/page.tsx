import { RunsClient } from './runs-client'

/**
 * Runs (Operations) Page
 *
 * Client-rendered page that fetches operations from the API.
 */
export default function RunsPage() {
  return <RunsClient />
}
