# =============================================================================
# clawcontrol - Environment Configuration
# =============================================================================
# Copy this file to .env and update values as needed:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------------------------
# SQLite database with WAL mode for local-first performance.
# Path is relative to prisma/schema.prisma directory.
DATABASE_URL="file:../data/clawcontrol.db"

# -----------------------------------------------------------------------------
# DEVELOPMENT OPTIONS (Optional)
# -----------------------------------------------------------------------------
# Force mock data mode even if OpenClaw is available on PATH.
# Useful for UI development without executing real commands.
# USE_MOCK_DATA="true"

# Node environment: development | production | test
# NODE_ENV="development"

# -----------------------------------------------------------------------------
# WORKSPACE CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# OpenClaw workspace path - the directory containing agents/, skills/, memory/, etc.
# Set this to point clawcontrol at your workspace for file browsing.
# Can also be configured via Settings > Workspace in the UI.
# OPENCLAW_WORKSPACE="/path/to/your/openclaw-workspace"

# Log level: debug | info | warn | error
# LOG_LEVEL="info"

# Enable verbose command output in receipts
# VERBOSE_RECEIPTS="false"

# -----------------------------------------------------------------------------
# MAINTENANCE (Optional)
# -----------------------------------------------------------------------------
# Suggested polling interval (ms) for clients calling /api/maintenance.
# Server responses are cached for ~30s; clients should not poll more frequently.
# MAINTENANCE_POLL_INTERVAL_MS="30000"
